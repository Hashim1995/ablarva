import Email from '../entities/email/email';

/**
 * Renders the EmailPage page.
 *
 * @returns The rendered EmailPage component.
 */
function EmailPage() {
  return <Email />;
}

export default EmailPage;
