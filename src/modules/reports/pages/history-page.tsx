import History from '../entities/history/history';

/**
 * @description This component is stand for payment history page
 * @file This file defines the HistoryPage component. This component is the parent page for the history page. It contains the history component.
 * @returns JSX element representing the HistoryPage component.
 */
function HistoryPage() {
  return <History />;
}

export default HistoryPage;
